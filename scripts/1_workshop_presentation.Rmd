---
title: "<img src=\"https://crctcs.openum.ca/files/sites/60/2024/03/cuso2.png\" style=\"float: left;margin-right:25px; margin-top:7.5px; height:150px\"/> Workshop on Computational Text Analysis"
author: "Maxime Sainte-Marie"
date: "`r format(Sys.time(), '%B %d %Y')`"
output:
  html_document:
    toc: yes
    toc_float: yes
    css: '../scripts/css_files/standard.css'
geometry: margin=1in
knit: (function(inputFile, encoding) {
  browseURL(
    rmarkdown::render(
      inputFile,
      encoding = encoding,
      output_dir = 'manuscripts',
      output_file = "introduction.html"))})

---

```{r setup, include=FALSE}
# Make sure every package is installed
for(this_package in c("data.table",
                      'DT',
                      'gt',
                      "tidyverse",
                      "readxl",
                      'fs',
                      "rstudioapi",
                      'knitr',
                      'rprojroot')){
  if(!this_package %in% installed.packages()){
    install.packages(this_package)
  }
}

#activate magrittr to use pipe operator without referencing
library(magrittr)
library(data.table)

knitr::opts_chunk$set(
  echo = TRUE,
  root.dir = rprojroot::find_rstudio_root_file())

```

# Methodology

Here is the methodology.

Here is the distribution of the first name genderedness of all human entities in the truthy Wikidata dump downloaded on April 3rd, 2024.


## First-name-based gender probabilities

This is the procedure.

# Results

These are the results.

## Author Types

These are the results.

## Citations

These are the results.
